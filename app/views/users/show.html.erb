<%= render 'layouts/sidebar' %>

<main class="content">
  <h2><%= @user.name %></h2>
  <p>Email: <%= @user.email %></p>

  <% if @user == current_user %>
    <a href="<%= edit_user_url %>">Edit</a>
  <% end %>
</main>

<%= render 'layouts/footer' %>
